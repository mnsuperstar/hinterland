<%= semantic_form_for(resource, url: update_credit_admin_user_path(resource), method: :put) do |f| %>
  <%= f.inputs do %>
    <% f.input :credit_amount, input_html: { value: 0 }, hint: "Current credit: #{resource.credit_amount.format}" %>
  <% end %>
  <%= f.submit 'Add credit' %>
<% end %>
